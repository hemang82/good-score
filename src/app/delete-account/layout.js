export const metadata = {
  title: "Delete Account Request - UPSCORE Security",
  description: "Request permanent deletion of your UPSCORE profile data, credit records, and payment history securely.",
};

export default function DeleteAccountLayout({ children }) {
  return <>{children}</>;
}
